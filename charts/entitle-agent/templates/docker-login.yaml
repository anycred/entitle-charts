apiVersion: v1
kind: Secret
metadata:
  name: {{ include "entitle-agent.fullname" . }}-docker-login
  labels:
  {{- include "entitle-agent.labels" . | nindent 4 }}
data:
  .dockerconfigjson: {{ include "entitle-agent.finalImageCredentials" . }}
type: kubernetes.io/dockerconfigjson

# Workflow name!
name: 'security-scan'

# Events that trigger this workflow:
# - When a pull request is opened or updated
# - When manually triggered via the GitHub Actions UI
# - On a weekly schedule (every Sunday at midnight)
on:
  pull_request: {}
  workflow_dispatch: {}
  schedule:
    - cron: '0 0 * * 0'

permissions: {}

jobs:
  security-scan:
    name: 'BeyondTrust Security Scan'
    runs-on: ubuntu-latest
    environment: wiz
    if: ${{ github.actor != 'dependabot[bot]' }}

    # Set permissions for GitHub token
    permissions:
      security-events: write  # Needed to upload SARIF results to GitHub Security tab
      contents: read          # Allows the job to read repo content

    # Default shell for all steps in this job
    defaults:
      run:
        shell: bash

    steps:
      # Step 1: Checkout the repository code
      - name: Check out repository
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0  # Fetch full history for accurate scan results

      # Step 2: Run security scan (Wiz IaC scan via the unified security-scan-action)
      - name: Run security scan
        uses: BeyondTrust/security-scan-action@59273ef26ad3471198bcbcaca7ba0751467f3d94 # v1.3.0
        with:
          wiz-client-id: ${{ secrets.WIZ_CLIENT_ID }}
          wiz-client-secret: ${{ secrets.WIZ_CLIENT_SECRET }}
